@using ArtClub.Models.Entities
@using ArtClub.Models.Enums
@model ArtClub.Models.Entities.User

@{
    ViewData["Title"] = "Account details";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h1>Account details</h1>
        <p class="text-muted mb-0">Review user information, role, and status.</p>
    </div>
    <a asp-action="Index" class="btn btn-secondary">
        <i class="bi bi-arrow-left"></i> Back to accounts
    </a>
</div>

<div class="row">
    <div class="col-md-6">
        <div class="card mb-3 shadow-sm">
            <div class="card-header bg-dark text-white">
                <h2 class="h5 mb-0">General Information</h2>
            </div>
            <div class="card-body">
                <h3 class="h4">@Model.UserName</h3>
                <hr />
                <p class="mb-1"><strong>Email:</strong> @Model.Email</p>
                <p class="mb-1">
                    <strong>Role:</strong>
                    <span class="badge @(Model.Role == UserRole.Admin ? "bg-danger" : "bg-info")">
                        @Model.Role
                    </span>
                </p>
                <p class="mb-0">
                    <strong>Status:</strong>
                    <span class="badge @(Model.IsActive ? "bg-success" : "bg-secondary")">
                        @(Model.IsActive ? "Active" : "Inactive")
                    </span>
                </p>
            </div>
        </div>
    </div>

    @* --- SECȚIUNE SPECIFICĂ BAZATĂ PE ROL ȘI PROPRIETĂȚI --- *@
    <div class="col-md-6">
        @if (Model.Role == UserRole.Member)
        {
            <div class="card mb-3 border-info shadow-sm">
                <div class="card-header bg-info text-white">
                    <h2 class="h5 mb-0">Member / Artist Details</h2>
                </div>
                <div class="card-body">
                    @* Folosim proprietățile mutate în clasa User *@
                    <p class="mb-1"><strong>Member Since:</strong> @(Model.MembershipDate?.ToShortDateString() ?? "N/A")</p>
                    <p class="mb-1"><strong>Event Limit:</strong> @Model.EventCreationLimit</p>
                    <p class="mb-1"><strong>Membership Active:</strong> @(Model.IsMembershipActive ? "Yes" : "No")</p>
                    <p class="mb-0"><strong>Events Organized:</strong> @(Model.OrganizedEvents?.Count ?? 0)</p>
                </div>
            </div>
        }
        else if (Model.Role == UserRole.Admin)
        {
            <div class="card mb-3 border-danger shadow-sm">
                <div class="card-header bg-danger text-white">
                    <h2 class="h5 mb-0">Administrative Privileges</h2>
                </div>
                <div class="card-body">
                    @* Dacă ai păstrat câmpuri specifice de admin în User, le pui aici *@
                    <p class="mb-1"><strong>System Admin:</strong> Yes</p>
                    <p class="mb-0"><strong>Full Access:</strong> Guaranteed</p>
                </div>
            </div>
        }
    </div>
</div>

<div class="mt-3">
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Edit Account</a>
    <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-outline-danger">Delete</a>
</div>